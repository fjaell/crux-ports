# Description: Kernel and mesa firmware for nouveau
# URL: http://nouveau.freedesktop.org/wiki/'
# Maintainer: Juergen Daubert at crux dot nu

name=nouveau-firmware
version=340.32
release=1
source=(https://raw.githubusercontent.com/envytools/firmware/master/extract_firmware.py
    http://us.download.nvidia.com/XFree86/Linux-x86/$version/NVIDIA-Linux-x86-$version.run)

build() {
    sh NVIDIA-Linux-x86-$version.run --extract-only
    /usr/bin/python3 extract_firmware.py
    install -d -m 0755 $PKG/lib/firmware/nouveau
    cp -a nv* vuc-* $PKG/lib/firmware/nouveau
}
